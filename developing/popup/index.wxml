<view
  class="jan-popup {{ show ? '' : 'jan-popup--hide' }}"
  style="z-index: {{ zindex }}; visibility: {{ _show ? 'visible' : 'hidden' }}; transition-duration: {{ duration / 1000 }}s;"
>
  <view class="jan-popup--overlay jan-popup--overlay--{{ position }}" catch:tap="onBgTap">
    <!-- 使用 catch 是为了阻止 overlay 层接收到点击事件 -->
    <view class="jan-popup--panel jan-popup--panel--{{ position }}" catch:tap="onPanelTap">
      <slot></slot>
    </view>
  </view>
</view>

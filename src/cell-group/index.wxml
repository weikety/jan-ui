<view class="jan-cell-group {{ customClass }}">
    <view class="jan-cell-group-container" style="{{ borderRadius ? 'border-radius: ' + borderRadius : '' }}">
        <slot></slot>
    </view>
</view>
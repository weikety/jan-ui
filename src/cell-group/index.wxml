<view class="joy-cell-group {{ customClass }}">
    <view class="joy-cell-group-container" style="{{ borderRadius ? 'border-radius: ' + borderRadius : '' }}">
        <slot></slot>
    </view>
</view>